import type { TCanvasNode } from '@interfaces';

import { createCanvasNode } from '../../utils';

export const answeredNodes: TCanvasNode[] = [
  createCanvasNode(
    'sb-resolution-answer',
    0,
    0,
    'Adopt qualified-majority voting for canvas evaluation.',
    null,
    [],
    false,
    true,
  ),
];
